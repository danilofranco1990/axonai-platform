# Arquivo de configura��o principal
# Ativa o perfil 'dev' por padr�o quando nenhum outro � especificado.
spring:
  profiles:
    active: dev

server:
  port: ${PORT:8080}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

openapi:
  info:
    title: AxonAI Platform API
    version: v0.0.1
    description: API para o orquestrador de projetos assistidos por IA.
    license:
      name: MIT License
      url: https://opensource.org/license/mit/